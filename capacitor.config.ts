import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'codes.narasima.app',
  appName: 'Accessibility App',
  webDir: 'dist/accessibility_app',
  bundledWebRuntime: true
};

export default config;
